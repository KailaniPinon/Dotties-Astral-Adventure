using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AI_DetectWall : MonoBehaviour
{
//   public bool isPlayerVisible = false;
//    public float wallDetRadius = 10.0f;
//    [SerializeField] LayerMask obstruction = 8;
//    [SerializeField] AIController scriptAIController;
////    [SerializeField] Transform player;

//    /* = = = = */ /* = = = = */ /* = = = = */ /* = = = = */ /* = = = = */

//    void Start() {
//        scriptAIController = GetComponent<AIController>();
//    }

    //public void DetectPlayer(Transform player) {
    //    RaycastHit hit;

    //    if (Physics.Raycast(transform.position, player.position, out hit)){ //Raycast is boolean: Is it within cast or not?
    //        isPlayerVisible = true;
    //    }
       //else isPlayerVisible = true;    //else if not looking at wall: isLooking... false

       // if (isPlayerVisible == true) {       //outdated/ triggered if AI is facing the wall
       //     scriptAIController.RunIdle();   //if looking at wall then execute RunIdle
       // }
   // }
}
